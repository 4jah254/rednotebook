# Installing on Apple Silicon Macs

Maybe I should have a script that checks for all these dependencies.

brew install pkg-config
brew install gettext (possibly to be already installed)
brew install glib
brew install cairo
brew reinstall gobject-introspection
brew install py3cairo
brew install gtk+3
brew install gtk-doc


The following should already be installed.
brew install libffi 
brew reinstall gobject-introspection




Dependencies for pygobject

brew install meson
brew install ninja

---
