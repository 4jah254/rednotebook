


Start by creating a new virtual environment with Python 3.11.7

`python3.11 -m venv .venv`

Install the dependencies from the requirements.txt file

`pip3.11 install -r requirements.txt`


Successful brew installations:
a) brew install cairo
b) brew install pygobject3
c) brew install libffi
d) brew install gtk+3
e) brew install gtksourceview3
f) brew install librsvg

Successful Pip installations:
i) pip install pycairo
ii) Run the command: export PKG_CONFIG_PATH="/usr/local/opt/libffi/lib/pkgconfig" and pip3.11 install PyGObject
iii) pip install PyYAML
iv) pip install PyGObject-stubs==0.0.8


export PKG_CONFIG_PATH="/usr/local/lib/pkgconfig:$PKG_CONFIG_PATH"
export PYTHONPATH="/usr/local/lib/python3.10/site-packages:$PYTHONPATH"

brew install cairo gobject-introspection

pip install PyGObject --verbose



After installing libffi, ran the following command in bashrc.
export PKG_CONFIG_PATH="/usr/local/opt/libffi/lib/pkgconfig"
export PKG_CONFIG_PATH="${PKG_CONFIG_PATH}:/usr/local/opt/libffi/lib/pkgconfig"
export PKG_CONFIG_PATH="/usr/local/opt/libffi/lib/pkgconfig" pip install PyGObject

icu4c is keg-only, which means it was not symlinked into /usr/local,
because macOS provides libicucore.dylib (but nothing else).

If you need to have icu4c first in your PATH run:
  echo 'export PATH="/usr/local/opt/icu4c/bin:$PATH"' >> ~/.zshrc
  echo 'export PATH="/usr/local/opt/icu4c/sbin:$PATH"' >> ~/.zshrc

For compilers to find icu4c you may need to set:
  export LDFLAGS="-L/usr/local/opt/icu4c/lib"
  export CPPFLAGS="-I/usr/local/opt/icu4c/include"

For pkg-config to find icu4c you may need to set:
  export PKG_CONFIG_PATH="/usr/local/opt/icu4c/lib/pkgconfig"


  --Provide local information:

  echo 'export LC_ALL=en_US.UTF-8' >> ~/.bash_profile
  echo 'export LANG=en_US.UTF-8' >> ~/.bash_profile




  brew install libsvg  --> libsvg-0.1.4_1.mojave.bottle.1.tar.gz

There is a virtualenv running on Python 3.6.0
Activate the virtualenv then call the script ./rednotebook

pip install PyGObject-stubs (This is a dev environment library)


==> libffi
libffi is keg-only, which means it was not symlinked into /usr/local,
because macOS already provides this software and installing another version in
parallel can cause all kinds of trouble.

For compilers to find libffi you may need to set:
  export LDFLAGS="-L/usr/local/opt/libffi/lib"
  export CPPFLAGS="-I/usr/local/opt/libffi/include"

For pkg-config to find libffi you may need to set:
  export PKG_CONFIG_PATH="/usr/local/opt/libffi/lib/pkgconfig"


brew tap jcudit/homebrew-webkitgtk
brew install --HEAD jcudit/webkitgtk/webkitgtk or brew install webkitgtk
